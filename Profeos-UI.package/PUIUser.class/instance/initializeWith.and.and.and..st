initialization
initializeWith: anId and: aUsername and: anEmail and: aPassword

	"Initialize this user with an id, username, email and password"

	self id: anId.
	self username: aUsername.
	self email: anEmail.
	self password: aPassword